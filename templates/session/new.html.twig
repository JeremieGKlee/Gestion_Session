{% extends 'base.html.twig' %}

{% block title %}création d'une nouvelle session{% endblock %}

{% block body %}
    
    <div class="container mt-4">
        <h1>Créer une nouvelle session</h1>
        {{ include ('session/_form.html.twig', {button: 'Créer'}) }}
        <div class="d-flex justify-content-end">
            <a href="{{ path ('session.index') }}" class="btn btn-secondary">Retour</a>
        </div>
    </div>
    

{% endblock %}